version: '3.9'

services:

  # ==================== NGINX ====================
  nginx:
    image: nginx:alpine
    container_name: caishenengine-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - fe
      - admin
      - transaction-api
      - docs
    networks:
      - caishenengine-net

  # ==================== FE (Homepage + Merchant Portal) ====================
  fe:
    build:
      context: ./fe
    image: fiqihqoin/caishenengine-fe:latest
    container_name: caishenengine-fe
    restart: always
    networks:
      - caishenengine-net

  # ==================== DOCS ====================
  docs:
    build:
      context: ./caishenengine-docs
    image: fiqihqoin/caishenengine-docs:latest
    container_name: caishenengine-docs
    restart: always
    networks:
      - caishenengine-net

  # ==================== LARAVEL ADMIN ====================
  admin:
    build:
      context: ./services/admin
    image: fiqihqoin/caishenengine-admin:latest
    container_name: caishenengine-admin
    restart: always
    env_file: .env.admin
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - caishenengine-net

  # ==================== LARAVEL QUEUE WORKER ====================
  admin-queue:
    build:
      context: ./services/admin
    image: fiqihqoin/caishenengine-admin:latest
    container_name: caishenengine-admin-queue
    restart: always
    command: php artisan queue:work --sleep=3 --tries=5 --max-time=3600
    env_file: .env.admin
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - caishenengine-net

  # ==================== GO TRANSACTION API ====================
  transaction-api:
    build:
      context: ./services/transaction-api
    image: fiqihqoin/caishenengine-go-api:latest
    container_name: caishenengine-transaction-api
    restart: always
    env_file: .env.go
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - caishenengine-net

  # ==================== POSTGRESQL ====================
  postgres:
    image: postgres:16-alpine
    container_name: caishenengine-postgres
    restart: always
    environment:
      POSTGRES_DB: caishenengine_db
      POSTGRES_USER: caishenengine_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-p5Nv5gM5aLycakSV}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U caishenengine_user -d caishenengine_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - caishenengine-net

  # ==================== REDIS ====================
  redis:
    image: redis:7-alpine
    container_name: caishenengine-redis
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - ./data/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - caishenengine-net

  # ==================== RABBITMQ ====================
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: caishenengine-rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: caishenengine
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-lhaN3cfjRM5MM3jK}
    volumes:
      - ./data/rabbitmq:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5
    networks:
      - caishenengine-net

networks:
  caishenengine-net:
    driver: bridge
