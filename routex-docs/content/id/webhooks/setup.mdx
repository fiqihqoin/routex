---
title: Setup Webhook
description: Langkah-langkah mendaftarkan endpoint webhook di Routex
lastUpdated: '2026-05-10'
---

# Setup Webhook

Ikuti langkah berikut untuk mulai menerima notifikasi Routex.

## 1. Siapkan Endpoint di Server Anda

Buat endpoint yang menerima request `POST` dengan body JSON.

## 2. Daftarkan URL di Portal

Buka [Dashboard Routex → Settings → Webhooks](https://app.routex.id/portal/webhooks) dan masukkan URL server Anda.

## 3. Verifikasi Endpoint

Routex akan mengirimkan test ping saat Anda menyimpan URL. Pastikan server Anda mengembalikan status `200 OK`.

<Callout type="warning">
Selalu gunakan HTTPS untuk endpoint webhook Anda guna menjaga keamanan data transaksi.
</Callout>
