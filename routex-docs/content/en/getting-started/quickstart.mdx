---
title: Quick Start
description: Start accepting QRIS payments via Routex in 5 minutes
lastUpdated: '2026-05-10'
---

# Quick Start

This guide will walk you through generating your first QRIS code 
through Routex in just 5 minutes.

## Prerequisites

- An active Routex account 
  ([Register here](https://app.routex.web.id/register))
- At least one vendor configured in the 
  Routex portal
- A sandbox API Key (received after account approval)

## Step 1 — Get your API Key

Once your account is approved, you will receive an email 
containing two API keys:

- **Sandbox Key** — starts with `ptms_sb_`
- **Production Key** — starts with `ptms_live_`

For development, please use the sandbox key first.

<Callout type="warning">
Never expose your API key in client-side code 
or public repositories. Use environment variables.
</Callout>

## Step 2 — Generate QRIS

<ApiEndpoint method="POST" endpoint="/api/v1/transactions" />

Your first request:

```bash
curl -X POST https://sandbox.routex.web.id/api/v1/transactions \
  -H "Content-Type: application/json" \
  -H "X-API-Key: ptms_sb_your_key_here" \
  -H "X-Idempotency-Key: order-12345" \
  -d '{
    "amount": 50000,
    "currency": "IDR",
    "payment_channel": "qris"
  }'
```

Successful response:

```json
{
  "transaction_id": "ptms_20260510_a1b2c3d4",
  "status": "pending_payment",
  "qris_code": "00020101021226...",
  "amount": 50000,
  "currency": "IDR",
  "payment_channel": "qris",
  "expires_at": "2026-05-10T14:47:00Z",
  "created_at": "2026-05-10T14:32:00Z"
}
```

## Step 3 — Check Transaction Status

```bash
curl https://sandbox.routex.web.id/api/v1/transactions/ptms_20260510_a1b2c3d4 \
  -H "X-API-Key: ptms_sb_your_key_here"
```

## Step 4 — Setup Webhook (Optional)

Register a webhook URL in the [Routex portal](https://app.routex.web.id/portal/webhooks) 
to receive notifications when a payment is completed.
